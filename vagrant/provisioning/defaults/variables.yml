hostname: EgzOnline

mysql_root_user: root
mysql_root_pass: root
mysql_user: symfony
mysql_pass: symfony
mysql_db: projectdb
mysql_bind_address: 0.0.0.0  # Allows all connections, for Production it should be 127.0.0.1
mysql_key_buffer: 64M

pma_host: pma.egzaminy.dev

vhosts:
  [
    { name: 'egzaminy.dev', rootFolder: '/var/www/web'}
  ]
  
vhosts_other:
  [
    { name: pma_host, rootFolder: '/usr/share/phpmyadmin'} 
  ]
